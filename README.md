# minishop
MiniShop is a backend e-commerce app built with Spring Boot. It follows a layered architecture and provides RESTful APIs for users, products, carts, and orders. The system uses Spring Security with JWT for authentication and authorization, along with validation, pagination, and global exception handling.
